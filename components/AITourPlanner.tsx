
import React from 'react';

// This component is deprecated in favor of TourSelector.tsx
// Keeping it as a placeholder to ensure the build process doesn't fail if it's still in the file tree.

const AITourPlanner: React.FC = () => {
  return null;
};

export default AITourPlanner;
