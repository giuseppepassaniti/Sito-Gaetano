
// AI Service disabled in favor of static curated tours.
// This file is kept as a placeholder to prevent build errors if referenced elsewhere, 
// but it no longer imports external libraries.

export const generateTourItinerary = async () => {
  console.log("AI Planner is currently disabled.");
  return null;
};
